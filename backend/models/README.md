# Runtime model artifacts

Expected runtime files:
- mobilenetv3_small_int8.tflite
- model_metadata.json

Copy the prototype artifact during model integration and record its SHA-256 in model_registry.

The currently supplied prototype artifact is not target-validated.
